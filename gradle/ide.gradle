// support for eclipse and intellij project config generation

import org.gradle.plugins.ide.idea.model.*

apply plugin: 'eclipse'
apply plugin: 'idea'


eclipse {
  classpath {
    // classes output directory customization
    defaultOutputDir = file('build-eclipse')
  }
}


